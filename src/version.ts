// [NOTE] @Agent kept in sync with package.json by tests/version.test.ts — the release flow bumps
// package.json manually, so the test is what stops this constant from drifting.
export const VERSION = "2.2.1";

export const USER_AGENT = `scrapegraph-js/${VERSION}`;
